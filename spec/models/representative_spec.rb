# frozen_string_literal: true

require 'rails_helper'

describe Representative do
    describe 'gets representative data' do
        before :each do
            # mock_rep = double(Representative, name: 'Trump', title: 'President', party: 'rep', id: '1')
        end
        # it 'calls helper get_data' do
    end
end
